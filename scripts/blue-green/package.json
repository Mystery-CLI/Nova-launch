{
  "name": "nova-launch-blue-green",
  "version": "1.0.0",
  "description": "Blue-green deployment orchestrator for Nova Launch",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^1.6.0",
    "typescript": "^5.3.0",
    "vitest": "^1.6.0"
  }
}
